import { MainLayout } from "@/components/layouts/MainLayout";
import type { Metadata } from "next";
import "global.css";

export const metadata: Metadata = {
  title: "Movie reviews analyzer",
};

export default MainLayout;
